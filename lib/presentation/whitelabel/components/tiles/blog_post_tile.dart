import 'package:flutter/material.dart';

class BlogPostTile extends StatelessWidget {
  const BlogPostTile({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
